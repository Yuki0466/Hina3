# 🚨 快速修复指南

## 问题：首页不显示商品

### 步骤 1: 检查环境变量

在项目根目录创建 `.env` 文件：

```bash
cp .env.example .env
```

编辑 `.env` 文件，填入你的 Supabase 信息：

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

### 步骤 2: 设置 Supabase 数据库

1. 登录 [Supabase Dashboard](https://supabase.com/dashboard)
2. 选择你的项目
3. 在 SQL 编辑器中执行以下 SQL：

```sql
-- 检查表是否存在
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

4. 如果没有表，请执行 `database-schema.sql` 中的 SQL

### 步骤 3: 使用调试面板

启动开发服务器后，首页左下角会显示"调试面板"：

- ✅ 绿色表示正常
- ❌ 红色表示有问题

点击"展开"查看详细信息。

### 步骤 4: 快速添加测试数据

如果数据库为空，使用右下角的"数据初始化"按钮添加示例数据。

### 常见问题

#### 问题 1: "未配置" Supabase
**解决**: 创建 `.env` 文件并填入正确的 URL 和 Key

#### 问题 2: "数据库连接失败"
**解决**: 
1. 检查 Supabase URL 是否正确
2. 检查 Anon Key 是否有效
3. 确认 Supabase 项目是激活状态

#### 问题 3: "表不存在"
**解决**: 在 Supabase SQL 编辑器中执行 `database-schema.sql`

#### 问题 4: 数据为空
**解决**: 使用"数据初始化"按钮添加示例数据

### 手动测试

在浏览器控制台中运行：

```javascript
// 检查环境变量
console.log('Supabase URL:', import.meta.env.VITE_SUPABASE_URL)
console.log('Has Key:', !!import.meta.env.VITE_SUPABASE_ANON_KEY)

// 测试数据库连接
import { supabase } from './src/services/supabase.js'
supabase.from('products').select('count').then(console.log)
```

### 完整设置检查清单

- [ ] 创建了 `.env` 文件
- [ ] 配置了 Supabase URL
- [ ] 配置了 Anon Key
- [ ] 执行了数据库 schema
- [ ] 重启了开发服务器
- [ ] 检查了调试面板状态

---

如果仍有问题，请：
1. 查看浏览器控制台错误
2. 检查调试面板信息
3. 确认 Supabase 项目设置