# 电商展示网站

基于 React、TypeScript、Supabase 和 Netlify 构建的现代化电商平台。

## 🛠️ 技术栈

- **前端**: React 18 + TypeScript + Vite
- **样式**: Tailwind CSS + Heroicons
- **后端**: Supabase (PostgreSQL + 实时订阅 + 认证)
- **部署**: Netlify (静态托管 + Serverless Functions)

## 📋 功能特性

### 已实现功能
- ✅ 用户认证系统 (注册/登录)
- ✅ 商品展示和搜索
- ✅ 商品详情页面
- ✅ 购物车管理
- ✅ 用户中心
- ✅ 订单管理
- ✅ 响应式设计

### 核心页面
1. **首页** - 商品轮播、分类展示、热门商品
2. **商品详情页** - 商品信息、规格选择、加购功能
3. **购物车页面** - 商品管理、数量调整、价格计算
4. **用户中心** - 个人信息、订单查看、地址管理

## 🗄️ 数据库设计

### 核心数据表
1. **products** - 商品信息表
2. **categories** - 商品分类表
3. **profiles** - 用户资料表
4. **orders** - 订单表
5. **order_items** - 订单详情表
6. **cart_items** - 购物车表

详细表结构请参考 [database-schema.sql](./database-schema.sql)

## 🚀 快速开始

### 环境准备
- Node.js 18+
- Git
- Supabase 账户
- Netlify 账户

### 1. 克隆项目
```bash
git clone <your-repo-url>
cd 202504
```

### 2. 安装依赖
```bash
npm install
```

### 3. 配置环境变量
复制环境变量模板：
```bash
cp .env.example .env.local
```

编辑 `.env.local` 文件，填入你的 Supabase 配置：
```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

### 4. 设置 Supabase 数据库
1. 在 Supabase Dashboard 创建新项目
2. 在 SQL 编辑器中执行 [database-schema.sql](./database-schema.sql) 中的 SQL 语句
3. 在 Authentication 设置中启用 Email/Password 登录
4. 在 Settings > API 中获取你的项目 URL 和 anon key

### 5. 本地开发
```bash
npm run dev
```

访问 http://localhost:3000 查看应用

## 🌐 部署到 Netlify

### 自动部署 (推荐)
1. 将代码推送到 GitHub/GitLab
2. 在 Netlify 中连接代码仓库
3. 配置环境变量 (在 Site settings > Build & deploy > Environment)
4. 部署设置将自动使用 `netlify.toml` 配置

### 手动部署
1. 构建项目：
```bash
npm run build
```

2. 上传 `dist` 文件夹到 Netlify

### 环境变量配置
在 Netlify 控制台中设置：
- `VITE_SUPABASE_URL`: 你的 Supabase 项目 URL
- `VITE_SUPABASE_ANON_KEY`: 你的 Supabase 匿名密钥

## 📁 项目结构

```
src/
├── components/         # 可复用组件
│   ├── Header.tsx    # 页面头部导航
│   └── ProductCard.tsx # 商品卡片
├── pages/             # 页面组件
│   ├── Home.tsx       # 首页
│   ├── ProductDetail.tsx # 商品详情
│   ├── Cart.tsx       # 购物车
│   ├── Auth.tsx       # 登录注册
│   ├── Profile.tsx    # 用户中心
│   └── Orders.tsx     # 订单列表
├── hooks/             # 自定义 Hooks
│   ├── useAuth.ts     # 认证相关
│   └── useCart.ts     # 购物车相关
├── services/          # 服务层
│   ├── supabase.ts    # Supabase 客户端
│   └── api.ts         # API 调用
├── types/             # TypeScript 类型定义
└── utils/             # 工具函数
```

## 🔧 开发命令

```bash
# 安装依赖
npm install

# 开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint

# 代码格式化
npm run format
```

## 🎨 自定义样式

项目使用 Tailwind CSS，配置文件为 `tailwind.config.js`。主要设计变量：

- 主色调：蓝色系 (`primary-500`, `primary-600`)
- 辅助色：绿色、橙色、红色用于不同状态
- 响应式断点：sm (640px), md (768px), lg (1024px), xl (1280px)

## 🔐 认证系统

使用 Supabase Auth 实现：
- 邮箱密码注册/登录
- JWT Token 管理
- 用户会话持久化
- 行业级安全策略 (RLS)

## 🛒 购物流程

1. 浏览商品 → 2. 查看详情 → 3. 加入购物车 → 4. 查看购物车 → 5. 结算 → 6. 查看订单

## 📱 响应式设计

- 移动端优先设计
- 平板和桌面端适配
- 触摸友好的交互
- 流畅的动画效果

## 🔄 数据流

```
用户界面 → React Hooks → API Service → Supabase Client → PostgreSQL
```

## 🚧 未来功能计划

- [ ] 支付集成 (Stripe/支付宝)
- [ ] 商品评价系统
- [ ] 优惠券功能
- [ ] 库存管理后台
- [ ] 实时聊天客服
- [ ] PWA 支持
- [ ] 多语言支持

## 📄 许可证

MIT License

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📞 支持

如有问题，请：
1. 查看 [FAQ](./FAQ.md)
2. 搜索 [Issues](../../issues)
3. 创建新的 Issue

---

**技术栈说明：**
- **Vite** - 现代化构建工具，提供快速的冷启动和热更新
- **TypeScript** - 类型安全，提高代码质量和开发体验
- **Tailwind CSS** - 实用优先的 CSS 框架，快速构建美观界面
- **Supabase** - 开源的 Firebase 替代品，提供数据库、认证和实时功能
- **Netlify** - 现代化的静态网站托管和 Serverless 平台